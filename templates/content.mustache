{{!
    @template block_hero/content

    Hero block content template.

    Example context (json):
    {
        "title": "Welcome to the Course Portal"
    }
}}
<div class="block-hero">
    {{#title}}
        <h1 class="hero-title">{{title}}</h1>
    {{/title}}
</div>
